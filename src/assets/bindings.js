const bindings = (function(){
  const model = new model
  const view = new view
  const controller = new controller
  return {
    model : model,
    view : view,
    controller : controller
  }
})()
